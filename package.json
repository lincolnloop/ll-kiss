{
  "name": "ll-kss",
  "version": "1.0.0",
  "description": "The kss-node template for Lincoln Loop's pattern libraries",
  "main": "template_config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile-scss": "./node_modules/.bin/node-sass --output-style compressed public/kss.scss public/kss.css"
  },
  "devDependencies": {
    "node-sass": "^3.4.2"
  }
}
